# Pooled application connection (for example, a Supabase transaction pooler).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/DATABASE?pgbouncer=true"

# Direct/session connection used by Prisma migrations and introspection.
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"
